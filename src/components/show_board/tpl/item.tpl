<div class="card {{ isFirst }}">
  <a href="./detail.html?id={{ id }}" target="_black">
    <div>
      <img src="{{ pic }}" alt="{{ phone_name }}"/>
    </div>
    <h3 class="title">{{ phone_name }}</h3>
    <p class="price">{{ default_price }}起</p>
  </a>
</div>